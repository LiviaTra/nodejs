let a = 0;
let b = 0;

const operacoes = require('./operacoes');

operacoes.soma(a,b); // imprime a soma de a e b no console

operacoes.subtracao(a,b); // imprime a subtração de a e b no console

operacoes.multiplicacao(a,b); // imprime a multiplicação de a e b no console

operacoes.divisao(a,b); // imprime a divisão de a e b no console

operacoes.resto(a,b); // imprime o resto de a e b no console

operacoes.soma(a,b); // imprime a exponenciação de a e b no console